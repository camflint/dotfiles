# Editing.
alias v=vim
alias fm='vifm .'

# Files.
alias f=find
alias rg='rg --hidden --smart-case'
alias ls='exa --icons'
alias la='ls -a'
alias ll='ls -l'
alias lla='ls -al'

# Git.
alias gc='git commit -m'
alias gs='git status'
alias ga='git add'
alias gd='git diff'

# Docker.
alias d=docker
alias dc='docker container'

# Tmux (deprecated).
alias nater='tmuxinator'

